app:
  name: AIPM
  version: 0.1.0-alpha.1

storage:
  root: ~/.aipm
  cache: ~/.aipm/cache
  models: ~/.aipm/models
  loras: ~/.aipm/loras
  workflows: ~/.aipm/workflows
  outputs: ~/.aipm/outputs
  logs: ~/.aipm/logs

download:
  workers: 8
  timeout: 300
  retries: 3
  verify_sha256: true